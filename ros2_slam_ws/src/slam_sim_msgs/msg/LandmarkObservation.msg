# A single range-bearing observation of a landmark whose identity
# has already been resolved by the (CNN-based) landmark classifier.
int32 id
float32 range      # metres
float32 bearing    # radians, relative to robot heading
